rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // NO RULES - Allow everything (for development/testing)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
